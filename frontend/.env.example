# Supabase Configuration
# Public URL - Safe to expose to client
NEXT_PUBLIC_SUPABASE_URL=your-project-url

# Anon/Public Key - Safe to expose to client
NEXT_PUBLIC_SUPABASE_ANON_TOKEN=your-anon-key

# Service Role Key - MUST BE KEPT SECRET! Server-side only!
# This key has admin privileges and should NEVER be exposed to the client
# Only used in API routes (/app/api/admin/*) for user management operations
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace the placeholder values with your actual Supabase credentials
# 3. Get your credentials from: https://app.supabase.com/project/YOUR_PROJECT/settings/api
# 4. NEVER commit .env.local to version control
# 5. The Service Role Key is required for user creation and invitation features

