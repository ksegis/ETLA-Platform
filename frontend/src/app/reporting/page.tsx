I have identified that the `reporting/page.tsx` file contains two component definitions, `AllReportsPage` and `EnhancedReportingPage`, which is causing a syntax error. I will remove the `AllReportsPage` component and make `EnhancedReportingPage` the default export to resolve this issue.
