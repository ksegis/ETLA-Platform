// src/rbac/constants.ts

// Re-export from the main constants file for consistency
export { FEATURES, PERMISSIONS, CORE_PERMISSIONS, ROLES } from './constants/index';

// Re-export types for convenience
export type { Feature, Permission, Role } from './constants/index';

